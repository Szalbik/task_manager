{% extends 'base.html.twig' %}

{% block body %}
    <h1>Comment creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul style="list-style: none; padding-left: 0; margin-top: 2px;">
        <li>
            <a class="btn btn-warning" href="{{ path('comment_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
